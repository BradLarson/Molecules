import SwiftUI

struct MoleculeMetadataView: View {

    var body: some View {
        VStack {
            Text("Hello world")
        }
        .padding()
    }
}

struct MoleculeMetadataView_Previews: PreviewProvider {
    static var previews: some View {
        return MoleculeMetadataView()
    }
}
